spring:
  application:
    name: greeting-service

  cloud:
    config:
      enabled: false

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'

greeting-service:
  greeting: "Hello"
  default-value: "World"
